<resources>

    <style name="AppTheme" parent="android:Theme.Holo.Light">
    </style>

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@android:color/black</item>
        <item name="android:layout_marginTop">@dimen/padding_small</item>
        <item name="android:layout_marginBottom">@dimen/padding_small</item>
    </style>

    <style name="Frame">
        <item name="android:background">@drawable/background_detail</item>
    </style>

    <style name="DetailFrame" parent="Frame">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">@dimen/padding_small</item>
        <item name="android:layout_marginBottom">@dimen/padding_small</item>
        <item name="android:padding">@dimen/padding_small</item>
    </style>

    <style name="DetailFrameHorizontal" parent="DetailFrame">
        <item name="android:orientation">horizontal</item>
    </style>

    <style name="DetailFrameVertical" parent="DetailFrame">
        <item name="android:orientation">vertical</item>
    </style>

    <style name="ProgressDialog" parent="android:Theme.Holo.Light.NoActionBar">
        <item name="android:windowBackground">@color/translucent_white</item>
    </style>

</resources>
